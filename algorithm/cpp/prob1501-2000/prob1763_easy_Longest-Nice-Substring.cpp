#include <string>
#include <vector>

using namespace std;

class Solution {
public:
    string longestNiceSubstring(string s) {
        int n = s.size();
        vector<int> flags(26);
        for(int i = 0; i < n; ++i)
        {
            //
        }
    }
};
