
#include <string>

using namespace std;
class Solution {
public:
    bool canConvertString(string s, string t, int k) {
        int ns = s.size(), nt = t.size();
        if(ns != nt) return false;

        for(int i = 0; i < ns; ++i)
        {
            char cs = s[i], ct = t[i];
        }
    }
};
